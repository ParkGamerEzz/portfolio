/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            { hostname: 'www.computerhope.com' }


        ],
    },
}

module.exports = nextConfig
